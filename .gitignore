# Build outputs
app/build/
app/.cxx/
app/release/
build/
.gradle/

# Android Studio
.idea/
*.iml
local.properties

# Secrets - never commit these
*.jks
google-services.json
*firebase-adminsdk*.json
secret.key
/data/
*.db
*.db-shm
*.db-wal
/avatars/
/files/
*.bak
*.bak*

# GDPR/trail export artifacts pulled from a device for verification — real
# location/message data, never a source file.
fshu_export_*.json

# Admin trail pulls from the live server (raw ciphertext, wrapped admin key, or
# decrypted points) — real user data, never a source file.
ivantrack*.json
tools/*_trail_bundle.json

# Trail viewer / admin exports — GPX is PLAINTEXT coordinates, JSON too; real
# location data, never a source file.
trail_export_*.gpx
trail_export_*.json

# Internal planning docs (Claude Code session files)
BRIEFING.md
4shu_master_plan.md
chat2_pre_ui_fixes.md
chat3_ui_rework.md
chat4_verify_and_polish.md

# Node
node_modules/

# Claude Code
.claude/

# OS
.DS_Store
Thumbs.db

# Screenshots (binary churn, no history value)
Screenshots/
